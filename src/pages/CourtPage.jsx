import React from 'react';
import AppealsConsole from '../features/governance/AppealsConsole';

const CourtPage = () => {
    return (
        <div className="page-container" style={{ maxWidth: '1200px', margin: '0 auto', paddingTop: '20px' }}>
            <AppealsConsole />
        </div>
    );
};

export default CourtPage;
